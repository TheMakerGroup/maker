tasks:
  build:
    - gcc -Wall -Wextra -O2 -o final main.c #Build command

  clean:
    - echo Cleaning up...
    - rm -f final

  run:
    - echo Running the program...
    - ./final
  
  default:
    - tasks.build
    - tasks.run